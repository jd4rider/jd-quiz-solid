import { Component, createSignal, createEffect, createRenderEffect } from 'solid-js';

import type { Cat } from './front';

const Quizbox: Component = () => {
  return <h1 class="text-6xl">Quiz has started</h1>
};

export default Quizbox;
